[project]
name = "psql-bastion"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.2.1",
    "psycopg[binary]>=3.2.10",
    "pyarrow>=21.0.0",
    "riffq>=0.1.6",
]
